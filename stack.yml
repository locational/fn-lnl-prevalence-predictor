provider:
  name: faas
  gateway: https://faas.srv.disarm.io
functions:
  fn-prevalence-predictor:
    lang: dockerfile
    handler: ./fn-prevalence-predictor
    image: disarm/fn-prevalence-predictor:0.1.5
    environment:
      exec_timeout: 500
      write_timeout: 510
      read_timeout: 510
      combine_output: false
      # write_debug: true
